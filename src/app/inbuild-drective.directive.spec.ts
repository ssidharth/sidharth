import { InbuildDrectiveDirective } from './inbuild-drective.directive';

describe('InbuildDrectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InbuildDrectiveDirective();
    expect(directive).toBeTruthy();
  });
});
