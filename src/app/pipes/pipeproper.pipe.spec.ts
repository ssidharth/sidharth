import { PipeproperPipe } from './pipeproper.pipe';

describe('PipeproperPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeproperPipe();
    expect(pipe).toBeTruthy();
  });
});
